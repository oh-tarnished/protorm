# Connection URL for the reserved_db database.
# Copy this file to .env and replace the value with your real credentials.
# reserved_db.config.ts reads it via env("RESERVED_DB_DATABASE_URL").
RESERVED_DB_DATABASE_URL="postgresql://user:password@localhost:5432/reserved_db"
