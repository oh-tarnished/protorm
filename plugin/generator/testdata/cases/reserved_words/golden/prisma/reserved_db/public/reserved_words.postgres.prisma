// Code generated by protoc-gen-protorm. DO NOT EDIT.
// versions:
// 	protoc-gen-protorm (unknown)
// 	protoc             (unknown)
// source: reserved_words.proto
//
// database: reserved_db
// schema:   public
//
// protorm — https://github.com/oh-tarnished/protorm

/// Status is the account state.
enum Status {
  /// Represents the unspecified value.
  UNSPECIFIED @map("unspecified")

  /// Represents the active value.
  ACTIVE @map("active")

  /// Represents the closed value.
  CLOSED @map("closed")

  /// Maps the enum to the "status" type in the database.
  @@map("status")
  /// Maps the enum to the "public" schema in the database.
  @@schema("public")
}

/// Account is forced onto the reserved table name "user" via a table override, with reserved-word columns and a composite UNIQUE index over them.
model Account {
  /// name: IDENTIFIER → PRIMARY KEY.
  name String @id @map("name")

  /// order is a reserved word; also carries a single-column index.
  order String? @map("order")

  /// select is a reserved word.
  select String? @map("select")

  /// status exercises a quoted, schema-qualified enum type reference.
  status Status @map("status")

  /// Unique constraint on [order, select].
  @@unique([order, select])
  /// Maps the model to the "user" table in the database.
  @@map("user")
  /// Maps the model to the "public" schema in the database.
  @@schema("public")
}
